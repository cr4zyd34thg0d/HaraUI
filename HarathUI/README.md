# HarathUI 😈✨ (v1.0.0)

## Your requested behaviors
- **XP bar**: At max level, shows watched Rep *only if a Rep is selected*; otherwise hides.
- **Cast bar**: Replaces Blizzard’s player cast bar (HarathUI hides it).
- **Loot toasts**: Shows **Items + Money + Currency** (toggles in options).

## Commands
- `/hui` → options
- `/hui lock` → toggle Move Mode
- `/hui xp | cast | menu | loot` → toggle modules

## Install / Update
Copy folder into:
`World of Warcraft/_retail_/Interface/AddOns/HarathUI/`
Then `/reload`


## 0.3.0 Cast Bar Changes
- Preview mode when Move Mode is ON (shows even when not casting).
- Square/flat icon crop + stronger frame styling.
- Text always drawn on top.


## 1.0.0
- Removed Frame/Health coloring module.
- Added Summoning Ring (Opie-inspired) for mounts and movement utilities. Use /hui ring.


## v1.1 Changes
- Sliders: centered label text, min/max hidden, live value shown
- Summoning Ring keybind added (Keybinds → HarathUI → Toggle Ring)
- First-pass Summoning Ring action editor (basic add/remove)
